export const { stderr, stdout } = process;
export const MAX_SCREEN_DIMENSION = 1000;

// render characters
export const ON_CHAR = "■";
export const OFF_CHAR = " ";

export const TERM_CHAR_ASPECT = 1.85; // the monospace font aspect ratio, roughly
