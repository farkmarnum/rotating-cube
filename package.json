{
  "name": "rotating-cube",
  "version": "0.2.2",
  "license": "MIT",
  "main": "./dist/main.js",
  "bin": {
    "rotating-cube": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node --watch index.mjs",
    "build": "esbuild index.mjs --bundle --minify --outfile=dist/main.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.4.0",
    "esbuild": "^0.21.4",
    "eslint": "9.x",
    "globals": "^15.4.0",
    "prettier": "^3.3.1"
  }
}
